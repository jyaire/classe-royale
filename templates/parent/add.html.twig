{% extends 'base.html.twig' %}

{% block title %}
    Classe Royale - Rattacher un enfant
{% endblock %}

{% block body %}
    <div class="col-12 col-md-7 col-lg-5 text-center">
        <h2 class="mb-4">Rattacher un enfant</h2>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="third-button">{{ button_label|default('Rattacher') }}</button>
        {{ form_end(form) }}
    </div>
    <div class="col-12 text-center">
        <a href="{{ path('home') }}">Retour</a>
    </div>

{% endblock %}
