<form method="post" action="{{ path('subject_delete', {'id': subject.id}) }}"
      onsubmit="return confirm('Voulez-vous vraiment supprimer ce thème ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subject.id) }}">
    <button class="danger-button">Delete</button>
</form>
