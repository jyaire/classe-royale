{% extends 'base.html.twig' %}

{% block title %}Classgroup{% endblock %}

{% block body %}
    <h1>Classgroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classgroup.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ classgroup.name }}</td>
            </tr>
            <tr>
                <th>Ref</th>
                <td>{{ classgroup.ref }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('classgroup_index') }}">back to list</a>

    <a href="{{ path('classgroup_edit', {'id': classgroup.id}) }}">edit</a>

    {{ include('classgroup/_delete_form.html.twig') }}
{% endblock %}
